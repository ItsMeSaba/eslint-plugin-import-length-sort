{
  "name": "eslint-plugin-import-length-sort",
  "version": "1.0.0",
  "description": "Sorts imports by line length (default-ish first, then named-only), longest to shortest.",
  "license": "ISC",
  "author": "Saba Silagadze",
  "keywords": [
    "eslint-plugin",
    "import",
    "sort",
    "import-length-sort"
  ],
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
